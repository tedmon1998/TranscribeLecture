# Веб-интерфейс для транскрибации

Веб-приложение для транскрибации лекций с использованием React + Vite + TypeScript на фронтенде и FastAPI на бэкенде.

## Структура проекта

```
lecture/
├── server.py              # FastAPI сервер
├── transcribe_lecture.py  # Логика транскрибации
├── frontend/              # React приложение
│   ├── src/
│   │   ├── App.tsx
│   │   └── components/
│   │       └── TranscriptionApp.tsx
│   └── package.json
└── requirements.txt
```

## Установка

### 1. Установка зависимостей Python

```bash
pip install -r requirements.txt
```

### 2. Установка зависимостей фронтенда

```bash
cd frontend
npm install
```

## Запуск

### 1. Запуск сервера (бэкенд)

В корневой директории проекта:

```bash
python3 server.py
```

Сервер запустится на `http://localhost:8000`

### 2. Запуск фронтенда

В директории `frontend`:

```bash
npm run dev
```

Фронтенд запустится на `http://localhost:5173` (или другом порту, который укажет Vite)

## Использование

1. Откройте браузер и перейдите на адрес фронтенда (обычно `http://localhost:5173`)
2. Выберите:
   - **Источник звука**: Микрофон или Системный звук
   - **Метод распознавания**: Whisper (Base/Small/Medium) или Google
   - **Язык**: Русский или English
3. Нажмите **"▶ Начать запись"**
4. Текст будет отображаться в реальном времени с логическими переносами строк
5. Используйте кнопки:
   - **📋 Копировать** - копирует текст в буфер обмена
   - **💾 Сохранить** - сохраняет текст в файл
   - **⏹ Остановить** - останавливает запись

## Особенности

- ✅ **Реальное время**: Текст появляется сразу после распознавания через WebSocket
- ✅ **Логические переносы строк**: Текст разбивается по предложениям, а не по сегментам
- ✅ **Кроссплатформенность**: Работает на macOS, Windows, Linux
- ✅ **Современный UI**: Адаптивный дизайн, работает на мобильных устройствах

## API Endpoints

### WebSocket

- `ws://localhost:8000/ws/transcribe/{session_id}` - WebSocket для транскрибации в реальном времени

### REST API

- `GET /api/health` - Проверка здоровья сервера
- `GET /api/devices` - Список доступных аудио устройств

## Разработка

### Сборка фронтенда для продакшена

```bash
cd frontend
npm run build
```

Собранные файлы будут в `frontend/dist/`

### Настройка CORS

В `server.py` можно настроить CORS для конкретных доменов:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],  # Указать конкретные домены
    ...
)
```

## Требования

- Python 3.8+
- Node.js 18+
- npm или yarn

## Примечания

- Для системного звука на macOS требуется BlackHole
- Первый запуск Whisper может занять время (загрузка модели)
- Google распознавание требует интернет-соединение

