# Транскрибатор лекций

Программа для автоматической транскрибации аудио лекций с учетом пауз и нескольких спикеров.

## Установка

```bash
pip install -r requirements.txt
```

## Использование

### 📹 Запись онлайн-лекций через ВКС (Google Meet, Zoom и т.д.)

**Основная команда для записи ВКС на русском языке (с системным распознавателем, как на телефоне):**

```bash
python3 transcribe_lecture.py --record --system-audio --use-system-recognizer -l ru
```

**С моделью Whisper (более точная, но медленнее):**

```bash
python3 transcribe_lecture.py --record --system-audio -m medium
```

**Для проверки доступных аудио устройств:**

```bash
python3 transcribe_lecture.py --list-devices
```

**Процесс работы:**

1. Запустите команду выше
2. Откройте вашу видеоконференцию (ВКС, Google Meet, Zoom)
3. Транскрипция сохраняется в файл в реальном времени
4. Нажмите `Ctrl+C` для остановки

**⚠️ Для macOS требуется BlackHole для записи системного звука:**

```bash
brew install blackhole-2ch
# Затем в Системных настройках → Звук выберите BlackHole
```

### 🎤 Запись с микрофона в реальном времени

**Основная команда для записи с микрофона (офлайн, русский язык - РЕКОМЕНДУЕТСЯ):**

```bash
python3 transcribe_lecture.py --record -m base -l ru
```

**С онлайн распознавателем Google (требует интернет, но быстрее):**

```bash
python3 transcribe_lecture.py --record --use-system-recognizer -l ru
```

**С более точной моделью Whisper (офлайн, медленнее, но точнее):**

```bash
python3 transcribe_lecture.py --record -m medium -l ru
```

**С более точной моделью Whisper:**

```bash
python3 transcribe_lecture.py --record -m medium -l ru
```

**Процесс работы:**

1. Запустите команду выше
2. Говорите в микрофон
3. Транскрипция сохраняется в файл в реальном времени
4. Нажмите `Ctrl+C` для остановки

**Примечание:** Без флага `--system-audio` программа записывает с микрофона по умолчанию.

### Обработка аудио файла

```bash
# Базовое использование
python3 transcribe_lecture.py audio_file.mp3

# Потоковая обработка (рекомендуется)
python3 transcribe_lecture.py audio_file.mp3 --streaming
```

### С выбором модели и языка

```bash
# Более точная модель (медленнее)
python3 transcribe_lecture.py --record -m medium

# Другой язык
python3 transcribe_lecture.py --record -l en

# Все параметры
python3 transcribe_lecture.py audio_file.mp3 \
    -o output.txt \
    -l ru \
    -m base \
    --streaming
```

## Основные параметры

- `-l, --language` - Язык (`ru` по умолчанию, `en`, и т.д.)
- `-m, --model` - Модель Whisper (`tiny`, `base`, `small`, `medium`, `large`)
- `--device` - Устройство для обработки (`cpu`/`cuda`/`mps`, автоматически определяется)
- `--record` - Запись с микрофона/динамиков в реальном времени
- `--system-audio` - Записывать системный звук (динамики)
- `--streaming` - Потоковая обработка файла в реальном времени
- `-o, --output` - Путь для сохранения результата
- `--use-system-recognizer` - Использовать системный распознаватель речи (как на телефоне) вместо Whisper
- `--recognizer-type` - Тип системного распознавателя (`google` - онлайн, `sphinx` - офлайн, по умолчанию: `google`)

## Использование GPU

**Программа автоматически определяет и использует GPU:**

- **Apple Silicon (M1/M2/M3)** → MPS (Metal) - **автоматически**
- **NVIDIA GPU** → CUDA - **автоматически**
- Если GPU не доступен → CPU

**Примечание для MPS (Apple Silicon):**

- Word timestamps автоматически отключаются (ограничение MPS)
- Это не влияет на качество транскрибации, только на детализацию временных меток слов

**Принудительное использование конкретного устройства:**

```bash
# Использовать CPU (даже если есть GPU)
python3 transcribe_lecture.py --record --system-audio --device cpu

# Использовать MPS (для Apple Silicon)
python3 transcribe_lecture.py --record --system-audio --device mps

# Использовать CUDA (для NVIDIA)
python3 transcribe_lecture.py --record --system-audio --device cuda
```

## Решение проблем

**Ошибка SSL при загрузке модели:**

```bash
/Applications/Python\ 3.11/Install\ Certificates.command
```

**Установка FFmpeg (не обязательно, но рекомендуется):**

```bash
brew install ffmpeg  # macOS
```

## Системный распознаватель речи (как на телефоне)

### Офлайн распознавание (как на iPhone)

**Для русского языка используйте Whisper (офлайн, работает БЕЗ интернета, как на iPhone):**

```bash
# Для записи с микрофона (офлайн, без интернета, русский язык)
python3 transcribe_lecture.py --record -m base -l ru

# Для записи ВКС (офлайн, без интернета, русский язык)
python3 transcribe_lecture.py --record --system-audio -m base -l ru
```

**⚠️ Важно:** PocketSphinx поддерживает только английский язык из коробки. Для русского языка на macOS используйте Whisper - он работает офлайн и поддерживает русский язык, как встроенный распознаватель на iPhone.

**Преимущества офлайн режима (Whisper для русского):**

- 📱 Работает БЕЗ интернета (как на iPhone)
- 🇷🇺 Поддерживает русский язык офлайн
- 🎯 Высокая точность распознавания
- 🔒 Приватность (все данные обрабатываются локально)
- ⚡ Быстро (особенно с GPU/MPS на Apple Silicon)

### Онлайн распознавание (Google)

**Требует интернет (как Google Assistant):**

```bash
# Для записи с микрофона (требует интернет)
python3 transcribe_lecture.py --record --use-system-recognizer -l ru

# Для записи ВКС (требует интернет)
python3 transcribe_lecture.py --record --system-audio --use-system-recognizer -l ru
```

**Преимущества онлайн режима (Google):**

- 🎯 Более точное распознавание
- 🌐 Поддержка многих языков
- ⚡ Быстро (использует облачные сервисы Google)

**Примечание:** Google Speech Recognition API бесплатный до определенного лимита запросов.

## Быстрый старт

**Запись с микрофона (офлайн, русский язык - РЕКОМЕНДУЕТСЯ):**

```bash
python3 transcribe_lecture.py --record -m base -l ru
```

**Запись с микрофона (онлайн, Google - требует интернет, но быстрее):**

```bash
python3 transcribe_lecture.py --record --use-system-recognizer -l ru
```

**Запись ВКС/системного звука (офлайн, русский язык):**

```bash
python3 transcribe_lecture.py --record --system-audio -m base -l ru
```

**Запись ВКС/системного звука (онлайн, Google - требует интернет):**

```bash
python3 transcribe_lecture.py --record --system-audio --use-system-recognizer -l ru
```

**С микрофона, как я и хотел:**

```bash
python3 transcribe_lecture.py --record --use-system-recognizer -l ru
```
